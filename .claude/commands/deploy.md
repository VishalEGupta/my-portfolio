Deploy the current state of the portfolio to GitHub Pages.

Steps to follow:
1. Run `git add .` to stage all changes
2. Run `git commit -m "$ARGUMENTS"` using the message provided
3. Run `git push` to push to GitHub
4. Confirm the push was successful and remind the user their site will update at https://vishalegupta.github.io/my-portfolio/ within 60 seconds

If no commit message was provided, use "update portfolio" as the default message.
